#include "main.h"
/**
 * _isupper - checks for uppercase letters.
 * @c - returns 1 if uppercase, returns 0 if lowercase, integer of above function..
 */

int _isupper(int c)
{
	if
	((c >= 'A') && (c <= 'Z'))
	return (1);
	else
	return (0);
}
