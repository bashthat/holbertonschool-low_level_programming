#include <stdio.h>
#include "main.h"
/**
 * main: the function adds @x and @z
 * add - adds the integers x and z
 * x - an integer to the equation.
 * z - the other integer of the equation
 * Return: always zero or one
 */
/*this is a program that is simple, a+b, in this case z and x*/
int add(int x, int z)
{
/*according to betty x and z are members... but of what?*/
return (x + z);
}
