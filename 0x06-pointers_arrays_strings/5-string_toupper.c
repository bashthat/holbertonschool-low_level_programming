#include "holberton.h"
/**
 * string_toupper - main
 * @s: string input
 * Return - zero
 */
char *string_toupper(char *)
{
int x = 0;

while (s[x] != '\0')
{
if (s(x) >= 97 && s[x] <= 122)
{
s(x) = (int)s[x] - 32;
}
x++;
}
return (0);
}
